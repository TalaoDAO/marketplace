<?php

/**
 * @file
 * Ethereum vote for Drupal.
 *
 * Features are in submodules.
 */

/**
 * Implements hook_menu().
 */
function ethereum_vote_menu() {
   $items = array();

   $items['admin/config/ethereum/vote'] = array(
     'title' => 'Ethereum',
     'description' => 'Ethereum vote.',
  );

  return $items;
}

